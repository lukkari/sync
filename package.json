{
  "name": "Lukkari-sync",
  "version": "0.0.1",
  "description": "Desktop application that syncs folder with web-version",
  "main": "index.js",
  "scripts": {
    "start": "DEBUG=true ../binaries/atom ./",
    "prod": "../binaries/atom ./",
    "test": "mocha"
  },
  "devDependencies": {
    "mocha": "^2.0.1"
  },
  "dependencies": {
    "csv-parse": "0.0.6"
  }
}
